import styled from 'styled-components';

const Section = styled.section`
  text-align: center;
`;

Section.Aside = styled.aside`
  margin-top: 8px;
`;

Section.Text = styled.p`
  color: #999;
  font-size: 12px;
`;

export default Section;
